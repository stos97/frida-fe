export default {
    workers(state) {
        return state.workers;
    },
    currentWorker(state) {
        return state.currentWorker;
    },
    workerServices(state) {
        return state.workerServices;
    },
}
